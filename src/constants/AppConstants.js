const keyMirror = require('../utils/keyMirror');

module.exports = keyMirror('APP', {
  SHOW_SIDEBAR: false,
  HIDE_SIDEBAR: false,
  MOVING_INTERNAL_ASSET: false,
  NOT_MOVING_INTERNAL_ASSET: false,
  CONSUME_FIRST_TIME_NAV: false
});
